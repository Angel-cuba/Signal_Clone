node_modules/
.expo/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# macOS
.DS_Store

# Environment variables — NUNCA subir al repo
.env
.env.local
.env.production

# Firebase native config files (contain API keys — add via EAS Secrets or CI env vars)
google-services.json
GoogleService-Info.plist

# EAS Submit — Google Play service account key (NEVER commit)
service-account.json

# Firebase emulator state
.firebase/
firebase-debug.log
firebase-debug.*.log
firestore-debug.log
ui-debug.log

# Cloud Functions dependencies
functions/node_modules/

# AI tools
.qodo/
.cursor/
.aider*
.idea/
.vscode/
