{
  "name": "signal-clone-functions",
  "description": "Firebase Cloud Functions for Signal Clone",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "serve": "firebase emulators:start --only functions,firestore",
    "preflight": "node -e \"const f=require('../.firebaserc');if(f.projects.default.includes('TODO'))throw new Error('ERROR: Fill in .firebaserc with your Firebase project ID before deploying');\""
  },
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "expo-server-sdk": "^3.14.0",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0"
  },
  "private": true
}
